# Copy to secrets.local.yaml and fill in your credentials.
# secrets.local.yaml is gitignored and merged automatically on every run.
corpus:
  sources:
    pubmed:
      email: "you@example.com"
      api_key: "your-ncbi-api-key"
